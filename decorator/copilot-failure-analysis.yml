# decorator/copilot-failure-analysis.yml
# Pipeline decorator — injects a post-job step that analyzes failures using GitHub Copilot.
#
# TEMPORARY: Using CmdLine@2 to debug decorator injection.
# If this step appears in the pipeline, decorator injection works
# and the problem is the custom task registration.

steps:
  - task: CmdLine@2
    condition: always()
    displayName: '🔍 Decorator Injection Test (CmdLine)'
    inputs:
      script: |
        echo "✅ Decorator injection is WORKING"
        echo "   This proves the decorator YAML is being picked up by ADO."
        echo ""
        echo "   Job status: $(Agent.JobStatus)"
        echo "   Pipeline: $(Build.DefinitionName)"
        echo "   Run: $(Build.BuildNumber)"
        echo ""
        echo "   Next step: Replace this with the real CopilotFailureAnalysis@0 task."
