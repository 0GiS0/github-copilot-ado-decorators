# Orchestration Log — McManus

**Timestamp:** 2026-03-03T13:00:00Z
**Agent:** McManus (DevOps)
**Spawned by:** User (Gisela Torres)

## Task

Create test/verification decorator and update ARCHITECTURE.md.

## Actions

1. Created `decorator/test-decorator.yml` — test decorator with `${{ if }}` compile-time gate on `COPILOT_TEST_MODE` variable
2. Design uses `CmdLine@2` inline script (no custom task) — prints extension name, org, project, pipeline, agent details
3. Runtime opt-out via `COPILOT_TEST_DECORATOR_DISABLED` variable
4. Updated ARCHITECTURE.md sections 4 and 5 with test decorator documentation
5. Created decision file `mcmanus-test-decorator.md` in decisions inbox

## Outcome

Test decorator created. Enables verification that the extension is correctly installed and decorator injection is working, without requiring a pipeline failure. Compile-time gate ensures zero impact on normal pipelines.

## Duration

Single session.
